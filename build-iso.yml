name: Build CarBerry OS ISO

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test build pipeline
        run: |
          echo "Инициализация среды для сборки CarBerry OS..."
          echo "Система готова к добавлению скриптов сборки!"
